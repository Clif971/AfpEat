@model IEnumerable<AfpEat.TypeCuisine>

@{
    ViewBag.Title = "TypeCuisine";
}

<h2>TypeCuisine</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Specialite)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Specialite)
        </td>
        <td>
            
            @Html.ActionLink("Details", "Details", new { id=item.IdTypeCuisine }) |
           
        </td>
    </tr>
}

</table>
